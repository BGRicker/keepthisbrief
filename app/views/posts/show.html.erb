<style type="text/css">
.showme{
  border: 1px solid black;
}

</style>

<div class="showme col-xs-10 col-xs-offset-1">
<h3><%= @post.name %> - <%= @post.rating %>/5</h3><br>
<h4><%= @post.review %></h4> <br>

<% if current_user && current_user == @post.user %>
<%= link_to 'Edit', edit_post_path(@post), :class => 'btn btn-primary' %>
<%= link_to 'Destroy', post_path(@post), :method => :delete, :data => {:confirm => 'Are you sure you want to delete this?'},:class => 'btn btn-danger' %>
<% end %>

</div>
